# Dependencias Semana 2 — CNN sobre FER-2013
# Probado en Python 3.10 - 3.12 sobre macOS Apple Silicon (M1 Max)
# Última verificación: mayo 2026

# Computación científica
numpy>=1.24,<2.0
pandas>=2.0

# Visualización
matplotlib>=3.7
seaborn>=0.12

# Machine Learning clásico (métricas, class_weight)
scikit-learn>=1.3

# Deep Learning
# En macOS Apple Silicon: tensorflow-macos + tensorflow-metal habilitan MPS (Metal).
# En otros entornos (Linux/Windows/Colab): tensorflow estándar.
tensorflow-macos>=2.15,<2.17 ; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow-metal>=1.1        ; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow>=2.15,<2.17       ; sys_platform != 'darwin' or platform_machine != 'arm64'

# Carga y manipulación de imágenes
pillow>=10.0

# Notebooks
jupyter>=1.0
ipykernel>=6.25
notebook>=7.0
